describe('Class', () => {
    it('should can be create class', () => {
        class Customer{

        }

        class Order{

        }

        const customer: Customer = new Customer();
        const order: Order = new Order();
    });
});